# RabTech Academy — Task 03

Feature Engineering & Preprocessing Pipeline.

Contents:
- `feature-engineering-preprocessing-pipeline.ipynb` — reusable Scikit-Learn preprocessing pipeline
- `customer-churn-training.csv` — dataset used by the notebook

The notebook performs train/test splitting before transformations, numeric imputation and scaling, categorical one-hot encoding, and exploratory correlation analysis.
